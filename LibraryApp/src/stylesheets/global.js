import {StyleSheet} from 'react-native';

export const Global = StyleSheet.create({
  verticalCenter: {
    justifyContent: 'center',
  },
  horizontalCenter: {
    alignItems: 'center',
  },
  textCenter: {
    textAlign: 'center'
  },
});
